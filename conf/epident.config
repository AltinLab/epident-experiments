profiles {
    gemini {

        workDir = "/scratch/$USER/work"
        
        process {
            withLabel: epident_local {
                executor = 'local'
                conda = "$HOME/miniconda3/envs/epident-experiments"
            }
        }
    }
}
